# Run artefacts. The written report is kept as the contract deliverable;
# screenshots and the raw dump are evidence for the run that produced them and
# would otherwise add megabytes per run to the repository.
results/*/screens/
results/*/raw.json
